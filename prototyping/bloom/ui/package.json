{
  "name": "bloom-ui",
  "version": "0.1.0",
  "description": "A warm hearth for bloom — the orchestrator layer of un",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run vite\"",
    "vite": "vite",
    "server": "node server/index.js",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "vite": "^5.0.12"
  }
}
